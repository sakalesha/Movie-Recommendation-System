{
  "name": "movie-recommendation-system",
  "version": "1.0.0",
  "description": "Full-stack Movie Recommendation System with ML",
  "scripts": {
    "backend": "npm start --prefix backend",
    "frontend": "npm run dev --prefix frontend",
    "ml": "cd ml && uvicorn ml_api:app --reload --port 8000",
    "full-stack": "concurrently \"npm run backend\" \"npm run frontend\" \"npm run ml\"",
    "install:all": "npm install --prefix backend && npm install --prefix frontend && pip install -r ml/requirements.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
